# V2.0.0 设计思路文档

> 本项目：VPS-Tailscale-DERP-AutoSetup
> 当前状态：v1.0.0 已作为历史存档保存在 main 分支（tag: `v1.0.0`，Release ✓）
> 本文件用于逐步沉淀 V2.0.0 重做的设计思路 —— 逐条记录 bob 的讲述 + 秘书的归纳，中间有问题随时讨论。

---

## 讨论记录

### 需求 01 — 运行方式与管理入口

**讲述（Bob）：**
还是想做成 `bash <(curl ...)` 运行脚本的方式，运行完出现菜单。在命令行也可以通过快捷键 `tderp` 直接启动菜单，为以后维护方便，或是更改参数方便。

**归纳：**
- 安装方式保持"一行命令"：`bash <(curl -sL ...)` 拉取即装，装完直接进入菜单
- 安装后注册 `tderp` 命令作为快捷入口，随时输入即打开管理菜单
- 核心目的：**后续维护和改参数要方便** —— 菜单化操作，不要求用户懂底层配置

**待确认：** 无

---

### 需求 02 — 菜单语言默认与切换

**讲述（Bob）：**
输入 `tderp` 快捷键后出现的终端菜单界面默认中文，第一选项就是可以切换英文。

**归纳：**
- 菜单界面默认语言：**中文**
- 菜单提供中/英文切换能力，且**切换语言作为菜单第一个选项**（易发现、排最前）

**待确认：** 无

---

### 需求 03 — 双安装方式选择

**讲述（Bob）：**
界面菜单有系统安装以及 Docker 安装选择。

**归纳：**
- 安装入口提供两种方式二选一：
  1. **系统安装**（原生二进制/systemd，走传统路径）
  2. **Docker 安装**（容器化部署）
- 菜单中需要明确的安装方式选择步骤

**待确认：** 无（已确认：两种方式装完后，管理入口统一为同一个 `tderp` 菜单）

---

### 需求 04 — 完整菜单结构

**讲述（Bob）：**
菜单包含：中英文切换、系统安装、Docker 安装、查看 derp 实时日志、重启、停止、显示 acl 配置、完全卸载、退出。

**归纳：** `tderp` 主菜单完整结构（自上而下）：

```
╔═══════════════════════════════════════════╗
║        Tailscale DERP 管理器               ║
║             tderp v2.0.0                   ║
╚═══════════════════════════════════════════╝

  状态: 🟢 运行中  |  域名/IP: derp.example.com:12345  |  证书: 320天

    1. 中英文切换
    2. Docker 安装         ← 自建 ghcr.io 镜像部署
    3. 查看实时日志
    4. 重启服务
    5. 停止服务
    6. 更新 derper          ← 手动版本检测 + 确认升级
    7. 显示 ACL 配置
    8. 完全卸载
    0. 退出
  ```

  - 所有项位于同一个主菜单（无二级入口）
  - Docker 安装为本项目唯一安装方式（自建镜像供应链）
  - 安装后通过该菜单统一管理

**待确认：** 无

---

### 需求 05 — 安装流程交互设计原则

**讲述（Bob）：**
主要在系统安装和 Docker 安装部分。使用者选择后要有中文提示，说明要输入什么、输入的值怎么来的。程序能自己检测到的（或能直接下载的），就默认用户同意，用户只需回车确认；万一检测不对用户可修改。安装过程中每一步都要推导并核对设计交互菜单。

**归纳：**
- 安装流程（系统安装 & Docker 安装）的每一步：
  1. **能自动检测的**（系统、架构、公网 IP、Go/Docker 是否已装等）→ 检测后作为默认值展示，用户**回车确认**即可；检测不准时用户可改
  2. **不能检测、必须用户提供的**（如域名）→ 中文提示清楚说明"要输入什么、这个值哪来的/用来干嘛"
  3. **能直接下载的**（Go 工具链、Docker 安装脚本等）→ 默认同意，回车确认即可
- 交互菜单的每一步（选项、默认值、提示文案）需要 **Bob 与秘书共同推导核对** 后再实现

**待确认：** 无（后续逐步推导安装分步交互，见下文"安装流程推导"章节）

---

### 需求 06 — DERP 升级机制

**讲述（Bob）：**
DERP 安装好后涉及后续升级的问题吗？

**查证（官方 derper README）：**
- 官方**没有预编译二进制 / 包管理器**，必须自行用 `go install tailscale.com/cmd/derper@latest` 构建更新
- 官方建议：升级频率跟随 Tailscale 客户端版本（Tailscale 更新频繁）
- 若启用 `--verify-clients`，derper 与同机 tailscaled 必须**同一 git revision**，需一起升级
- Docker 部署升级更简单：重拉镜像 + 重建容器

**归纳：**
- V2 菜单必须包含 **"更新 derper"** 操作项：
  - 系统安装 → 重编译 derper（goproxy 加速）→ 重启 systemd 服务
  - Docker 安装 → 拉取新镜像 → 重建容器
- 升级是运维闭环的一环，不可省略

**待确认：** 无

---

### 需求 07 — 自动检测升级机制 + Docker 与系统升级对比

**讲述（Bob）：**
想通过 bash <(curl) 安装脚本后就有自动写入系统、自动检测并升级的功能，可能实现吗？是不是 Docker 升级没有系统升级方便？

**查证：**
- 社区 derper 镜像 **fredliang/derper**：latest 2026-05-25 更新，100K+ 拉取，属最活跃
- Docker 升级 = pull 新镜像 + 重建容器（`docker compose pull && up -d`），无需 Go 环境

**归纳：**
- **自动检测更新**可行：脚本内置 version check，定时（cron/systemd timer）对比当前 derper 与最新版本
- **全自动静默升级**不建议默认：升级会让 DERP 瞬断，且 `--verify-clients` 要求版本匹配，容易升坏
- 契合交互原则的折中：检测到新版 → **提示用户** → 默认回车确认升级（可取消）
- **Docker 升级比系统安装更简单**：
  - Docker：拉镜像 + 重建容器（1 条命令）
  - 系统安装：`go install` 重编译 + 重启服务（需 Go 环境 + GOPROXY 全套链路）
- 结论：**Docker 方案应作为 V2 的主力部署方式**（升级/维护都省），系统安装作备选

**待确认：**
- 自动检测频率？（如每天一次 cron / systemd timer）——待 Bob 定
- "自动检测 + 提示升级"的模式，确认采用？（基于交互原则，推测是）

---

### 需求 08 — 放弃自动检测，改为手动升级菜单（定稿）

**讲述（Bob）：**
不用自动检测了，用户登录 VPS 的机率不高，登录了肯定会运行我们的脚本检测这台 DERP 的状态。脚本内设置一个自动升级菜单可以吧。

**归纳（最终方案）：**
- ❌ **放弃**定时自动检测（cron/systemd timer 都不做）——理由：用户登录 VPS 频率低，无需后台常驻
- ✅ 用户登录后运行 `tderp` → 菜单里执行手动升级项时，脚本**实时检查** derper 最新版本并提示：当前版本 → 最新版本 → 回车确认升级
- 即：**手动触发时才检测**，升级动作仍走"提示 + 确认"
- **菜单项"更新 derper"（需求 06）保留**，行为 = 实时检查版本 + 确认后升级

**待确认：** 无（需求 07 的自动检测部分作废，仅保留"手动升级菜单"）

---

### 需求 09 — 自建 derper 镜像供应链（GitHub Actions + ghcr.io）

**讲述（Bob）：**
别人的镜像万一哪天不维护了，脚本就死了。能不能 V2 自己建立一个每月检测 DERP 新版本，有新版就利用 GitHub 打包运行做成镜像，放在自己项目的 Packages 里。但涉及 fork 问题：如果别人 fork 到自己仓库，Docker 路径就要相应改变。

**技术验证：**
- GitHub Actions 的 `schedule` 事件支持 cron 定时触发 ✅
- ghcr.io 的 public 包无需认证即可 pull（不限制拉取方）✅
- 包路径规则：`ghcr.io/${{ github.repository }}/derper`（与仓库绑定）✅
- 官方无 derper Dockerfile（404），需自行编写多阶段构建 ✅

**归纳（最终方案）：**
- 项目内编写 `Dockerfile`（多阶段构建：Go 编译 derper + 最小运行镜像）
- 项目内编写 `.github/workflows/build-derper-image.yml`：
  - **定时触发**（如每周检查一次 tailscale 新 tag）
  - 自动检测 tailscale 最新版本，与当前镜像版本对比
  - 有新版本时自动构建并推送镜像到 **ghcr.io/bobvane/VPS-Tailscale-DERP-AutoSetup/derper**
  - 多标签：`latest` + 版本号（如 `v1.72.0`）
- 脚本默认镜像源 = `ghcr.io/bobvane/VPS-Tailscale-DERP-AutoSetup/derper:latest`
- **fork 后镜像路径问题**：在 README 中说明，不写自动检测逻辑，用户 fork 后：
  1. 在 fork 仓库开启 GitHub Actions（默认关闭）
  2. Actions 会自动构建镜像到自己的 ghcr.io
  3. 在脚本菜单中修改镜像源变量为自己的路径
- **结论：V2 只保留 Docker 安装（自建镜像），弃用系统安装**（Go 编译链在国内维护成本高，且自建镜像已解决供应链痛点）

**待确认：** 无

---

### 需求 10 — 中英文联动镜像源（中文安装多一步镜像加速地址）

**讲述（Bob）：**
中英文切换不光切换语言，切换后还要具有拉取路径的不一样。中文安装多一个步骤：加速地址的步骤。给使用者提供一个默认地址、一个自定义。如果用户输入自定义，就把自定义的路径写入到脚本里面去固化，直到下次用户再启动安装选择加速地址可以更改。

**归纳：**
- **语言与镜像源联动**：
  - 🇨🇳 中文模式（默认）→ 安装流程多一个步骤：选择镜像源
  - 🌍 英文模式 → 直接拉 `ghcr.io` 原生，不需要此步骤
- **镜像源的交互方式**（中文模式安装时，三步选一）：
  1. **默认直连地址** `ghcr.io` — 适用于国内用户但买了国外 VPS 的情况
  2. **推荐的加速地址**（如 `ghcr.chenby.cn`）— 适用于国内 VPS 用户
  3. **自定义地址** — 用户自建加速站、企业镜像等
- 选择后**写入配置文件固化**（`tderp.env` 中的 `MIRROR_PREFIX`），下次安装可改
- 固化后的镜像源影响所有后续操作（拉取、更新）

**待确认：** 无

---

### 需求 11 — 证书方案三选一 + 安装说明文字

**讲述（Bob）：**
Let's Encrypt 自动证书和自签名证书都可以脚本自动实现吗？Let's Encrypt 自动证书可以脚本后实现自己一直后台监控，到期自动续签吗？如果都可以，我觉得应该在安装菜单上面做文字说明。并且在选用那个方案后屏幕也显示说明，比如 Let's Encrypt 自动证书模式，也以 CF 为例子，说明在 CF 把域名给 IP 做 DNS 绑定，要不要开小黄云等。

**技术查证（derper 源码 cert.go）：**
- **--certmode=letsencrypt**：使用 `golang.org/x/crypto/acme/autocert.Manager`，自动申请 LE 证书、缓存到 `--certdir`、**自动续期**（无需外部 cron/certbot）
- **--certmode=manual**：自签名模式，如果 `--certdir` 下无证书文件，derper 自动调用 `createSelfSignedIPCert()` 生成 ECDSA P256 自签名证书，有效期 1 年，到期自动重新生成
- **--acme-ip-certs**（新特性）：允许 LE 为纯 IP 地址签发证书，与 `--certmode=letsencrypt` 配合使用，无需域名
- 所有证书模式 derper 原生支持，脚本只需传参，无须额外工具

**归纳（最终方案）：**
- **安装流程中的证书步骤改为三选一**，**菜单上方显示各方案所需的端口开放说明**：
  1. **Let's Encrypt 自动证书（域名）** → `--certmode=letsencrypt --hostname=域名`
     - 需要域名，需 DNS A 记录指向 VPS IP
     - 可开启 `--verify-clients` 防白嫖
     - **需开放端口**：DERP端口(TCP) + STUN端口(UDP) + **80端口(TCP)** — LE HTTP-01 验证
     - 选后显示 CF 配置说明：**关闭小黄云（Proxy）**，DERP 需要直连
  2. **Let's Encrypt 自动证书（纯 IP）** → `--certmode=letsencrypt --acme-ip-certs --hostname=IP`
     - 无需域名，纯 IP 即可
     - 较新功能，实验性，但 derper 已原生支持
     - **需开放端口**：DERP端口(TCP) + STUN端口(UDP) + **80端口(TCP)**
     - 选后显示：确认 VPS 公网 IP 正确
  3. **自签名证书（域名/IP 均可）** → `--certmode=manual`
     - 无需域名，derper 自动生成自签名证书
     - 无法防白嫖（`--verify-clients` 需要域名模式）
     - **需开放端口**：DERP端口(TCP) + STUN端口(UDP)（**无需 80 端口**）
     - 选后显示：最简单模式，回车确认即可
- **交互规则**：菜单中显示简要文字说明（含端口要求），选中后屏幕打印详细说明（含 CF DNS 配置指引）
- **证书自动续期**：内置无需额外处理

**待确认：** 无

---

### 需求 12 — 安装前检测（DNS / 端口 / 输入验证）

**讲述（Bob）：**
我自己是阿里云的 VPS，之前做脚本的时候涉及到了服务器 DNS 的锁定问题，当然我是系统安装方式，现在用加速地址做 Docker 安装不知道会不会出现这个问题。另外脚本还要检测 DERP 端口不通不允许安装，输入 IP 和域名不对不能下一步。

**分析：**
- **阿里云 DNS 锁定**：系统安装时 `go install` 要拉 GitHub 源码 + Go 模块 → 阿里云 DNS 常常解析不了，需改 DNS 或设 GOPROXY。Docker 模式走国内镜像加速域名（如 `ghcr.chenby.cn`），这些是**国内 CDN 域名**，受 DNS 锁定影响极小。但为防万一，仍需检测
- **端口检测**：脚本在 VPS 内部运行，只能检测**端口是否被占用**（ss/lsof），**无法检测外部防火墙**是否放行（VPS 控制台的安全组只有用户自己知道），所以检测逻辑要区分
- **输入验证**：域名、IP、端口格式都可以用正则校验，不通过就不让下一步

**归纳（最终方案）：**

**① DNS 解析检测（安装前）**
- 安装前检测能否解析 ghcr.io 或所选加速地址
- 解析失败 → 提示用户，建议修改 DNS 为 `223.5.5.5`（阿里云公共DNS）或 `114.114.114.114`
- 检测通过才继续

**② 端口检测（安装前）**
- 检测 DERP 端口（TCP）和 STUN 端口（UDP）是否被占用
- **被占用** → 提示用户更换端口或关闭占用进程，阻止安装
- **未被占用** → 通过，继续安装
- 安装完成后，屏幕打印提示：**"请确保在 VPS 控制台安全组/防火墙中放行以下端口：DERP端口(TCP) + STUN端口(UDP) [+ 80端口(TCP) 仅LE模式]"**

**③ 输入验证（安装过程中）**
- **域名输入**：正则校验格式（`^[a-zA-Z0-9][a-zA-Z0-9.-]+\.[a-zA-Z]{2,}$`），不通过提示"域名格式不正确，请重新输入"
- **IP 输入**：正则校验 IPv4 格式，不通过提示"IP 地址格式不正确，请重新输入"
- **端口输入**：范围校验 1-65535，不通过提示"端口范围 1-65535，请重新输入"
- 验证不通过不允许下一步

**待确认：** 无

---

### 需求 13 — 各菜单操作细节 + docker-compose 模板 + 状态检测

**讲述（Bob）：**
docker-compose.yml 模板采用 Tailscale 的标准模板，目录、镜像名称等都用官方默认。重启服务是重启单独的 DERP 容器，别重启了别的容器。删除是把配置、容器、镜像、compose 和目录都删除，干净清除。配置文件按官方举例目录和路径。检测容器状态肯定是打开菜单后实时检测容器状态和域名 IP 状态。我觉得甚至证书过期时间也要显示出来。

**查证：**
- Tailscale 官方未提供标准 derper compose，但社区主流标准（kaaanata/derper-docker、cookding、sparanoid/docker-derp 等）高度一致，可视作"事实标准模板"
- 标准目录路径：`/app/certs` 为 derper 容器内证书目录，映射到宿主机 `./data/certs`
- 证书过期时间：可读取 cert 文件中的 `notAfter` 字段，通过 `openssl x509 -enddate -noout` 或解析 PEM

**归纳（最终方案）：**

**① docker-compose.yml 模板（事实标准）**
```yaml
services:
  derper:
    image: ghcr.io/bobvane/VPS-Tailscale-DERP-AutoSetup/derper:latest
    container_name: derper
    restart: unless-stopped
    ports:
      - "${DERP_PORT}:${DERP_PORT}"       # TCP
      - "${STUN_PORT}:${STUN_PORT}/udp"   # UDP
      - "80:80"                           # 仅 LE 模式需映射
    environment:
      - DERP_DOMAIN=${DERP_DOMAIN}
      - DERP_CERT_MODE=${DERP_CERT_MODE}
      - DERP_CERT_DIR=/app/certs
      - DERP_ADDR=:${DERP_PORT}
      - DERP_STUN=true
      - DERP_HTTP_PORT=80
      - DERP_VERIFY_CLIENTS=false
    volumes:
      - ./data/certs:/app/certs
```

**② 目录结构（标准路径）**
```
/opt/tderp/                  ← 安装目录（脚本安装时创建）
├── tderp                    ← /usr/local/bin/tderp 软链接
├── tderp.env                ← 配置文件（镜像源、语言等）
├── docker-compose.yml       ← 生成的 compose
└── data/
    └── certs/               ← 证书持久化目录
```

**③ 重启服务**
- 执行 `docker compose -f /opt/tderp/docker-compose.yml restart derper`
- 只重启 derper 容器，不动其他容器

**④ 完全卸载**
- `docker compose -f /opt/tderp/docker-compose.yml down`（停止并删除容器）
- `docker rmi ghcr.io/bobvane/.../derper`（删除镜像）
- `rm -rf /opt/tderp`（删除全部配置、compose、数据目录）
- `rm /usr/local/bin/tderp`（删除命令入口）
- 干净彻底，无残留

**⑤ 状态检测（打开菜单时实时检测）**
- 检测 `docker ps` 确认 derper 容器是否运行 → 🟢/🟡/🔴
- 检测域名/IP 解析：`curl -I --connect-timeout 3 https://DERP_DOMAIN:DERP_PORT` 或 `nc -zv`
- 检测证书过期时间：
  - 读取 `./data/certs/` 下证书文件
  - 用 `openssl x509 -in <cert> -noout -enddate` 解析 notAfter
  - 显示剩余天数（如"证书: 还有 320 天过期"）
- 状态行示例：
  ```
  状态: 🟢 运行中  |  域名/IP: derp.example.com:12345  |  证书: 320天
  ```

**⑥ 配置文件路径**
- 主配置：`/opt/tderp/tderp.env`（键值对格式，含镜像源、语言、端口等）
- 证书目录：`/opt/tderp/data/certs/`
- compose 文件：`/opt/tderp/docker-compose.yml`

**待确认：** 无

---

## 安装流程推导（待核对）

> 以下为秘书根据完整需求推导的安装步骤草案，**步步待 Bob 核对**。

### Docker 安装流程（唯一安装方式 — 自建 ghcr.io 镜像）

| 步骤 | 内容 | 值来源 | 交互方式 |
|------|------|--------|----------|
| *B0* | *DNS 解析检测* | *自动检测* | *检测能否解析所选镜像源，失败→提示改DNS，阻止安装* |
| *B0b* | *端口占用检测* | *自动检测 (ss/lsof)* | *检测 DERP/STUN 端口是否被占用，被占用→提示换端口，阻止安装* |
| B1 | 检测 Docker 是否已安装 | 自动检测 | 未装→提示是否自动安装 Docker（回车确认） |
| *B2* | *设置镜像加速地址（仅中文模式）* | *默认提供+用户可选自定义* | *显示默认加速地址，回车确认；或输入自定义地址* |
| B3 | 输入域名（如 derp.example.com） | 用户输入 | **正则校验格式**，不通过提示重输；中文提示说明用途 |
| B4 | 自动获取公网 IP | 自动检测（api.ip.sb 等） | 显示 IP，**正则校验格式**，回车确认（检测错可改） |
| B5 | DERP 端口 | 默认高位端口（如 12345） | 显示默认值，**范围校验 1-65535**，回车确认（可改） |
| B6 | STUN 端口 | 默认 3478 | 显示默认值，**范围校验 1-65535**，回车确认（可改） |
| B7 | 证书方式（三选一） | 用户选择 | 1. LE 自动证书(域名) 2. LE 自动证书(纯IP) 3. 自签名(默认)，选中后屏幕打印详细说明+CF配置指引 |
| B8 | 拉取镜像 | 自动拉 ghcr.io 自建镜像（走所设镜像源） | 进度显示，回车确认 |
| B9 | 生成 docker-compose.yml + 启动 | 自动 | 显示 compose 摘要，回车确认 |
| B10 | 输出 ACL 配置 + 端口提示 | 自动生成 JSON | 显示供复制，并提示在 VPS 控制台放行端口 |

> 所有决策已定，无待确认关键点。

---

## 设计缺口 — 已定稿

以下为 Bob 逐条确认后的最终方案：

| # | 缺口 | 最终决定 |
|---|------|---------|
| G1 | **verify-clients 防白嫖** | **默认不开启**，安装时询问用户。若选 Y：自动安装 tailscale 客户端 → 引导登录 tailnet → 挂载 socket 给 derper。若选 N：纯白嫖可忽略，因为 DERP 地址不公开，实际风险低 |
| G2 | **Docker 引擎自动安装** | 区分国内外：国外用官方脚本 `get.docker.com`，国内用清华镜像源或阿里云镜像安装。安装过程显示进度 |
| G3 | **更新 derper** | 拉新镜像 → `docker compose up -d` 重建容器，**不保留旧镜像** |
| G4 | **compose 模板差异** | **固定模板，不动态生成**。不同证书模式只改 tderp.env 中的变量值（`CERT_MODE`、`HTTP_PORT` 等），compose 模板本身不变 |
| G5 | **tderp.env 完整键列表** | 开发时列出，Bob 过目 |
| G6 | **日志查看** | `docker logs -f` 实时滚动，屏幕底部显示 `Ctrl+C 返回菜单` 提示 |
| G7 | **安装失败处理** | **自动回滚**：失败时清理已创建的 compose、配置、目录，提示错误信息 |
| G8 | **ACL 配置输出** | 开发时再定 |
| G9 | **多架构构建** | 开发时再定，建议 amd64 + arm64 都做 |